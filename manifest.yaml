name: Syncbot
layer: foundation
description: Backup, synchronization, and external API for robot-accessible storage.
capabilities:
  - sync_folders
  - backup_projects
  - expose_sync_api
depends_on:
  - Validator
provides:
  - SyncSpec
protocols:
  - sync.schema
working_hours: "24/7"
schema_version: "1.0"
